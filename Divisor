#include <stdio.h>
int main ()
{
  int n1, n2, m;
printf ("Ingresa 2 numeros\n");
scanf ("%d", &n1);
scanf ("%d", &n2);
m=n1%n2;
if (m==0)
{
printf ("%d es divisor de %d\n", n1, n2);
}
else
{
printf ("%d no es divisor de %d\n", n1, n2);
}
return 0;
}
